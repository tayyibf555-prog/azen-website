# BUILD BRIEF — azen full-site rebuild on berco structure
Overseer: Fable (session). Builder: subagent. BINDING — deviations need overseer sign-off.
Source of truth for berco patterns: `docs/research/berco-full-site-spec.md` (cite section numbers when you implement). Hero video mechanics in `berco-hero-spec.md` are REFERENCE ONLY — our hero is live Three.js, not video.

## 0. Final section flow (user-adjudicated — do not reorder)
nav / loader / 1 hero / 2 about+metrics / 3 case-study carousel / 4 founder / 5 services / 6 integrations / 7 mindset-sequence / 8 FAQ / 9 contact-footer.
- **NO pricing section anywhere** (user veto — overrides spec §9's recommendation).
- **Testimonials = case-study card flip-backs** (spec §9 row "Testimonials"): carousel cards flip (founders-flip mechanics §4) to a masked-grid back with a QUOTE PLACEHOLDER ("Client quote to be added — …") + "See full build →" link to that case page.
- **No decorative motion graphics at the bottom** — no rotating rings, no parallax ghost wordmark. Contact IS the footer (spec §8).

## 1. Global system (spec §0, azen-tokened)
- Page bg **#020202**, hidden scrollbars, `10vh 10vw` section rhythm, cards `#0f1011` + 1px white/5 hairline + r30 (20 mobile) + radial sheen (§4 recipe), vw type scale EXACTLY as §0.2.
- **Fonts (azen brand, berco roles)**: `.headline` = Geist Light 300 (the user-tuned thin voice); `.headline-bold` spans = Hanken Grotesk 700; all body/description classes = Hanken Grotesk 400/500; keep JetBrains Mono ONLY if something genuinely needs mono (default: don't). Drop Archivo/Schibsted/anything else.
- **Accent #0071e3** in berco's restraint: eyebrow pills (1vw×1vh) + the wordmark period. Nothing else colored.
- **Motion**: replicate §0.7 verbatim with **anime.js v4** (installed — use its `splitText`/`TextSplitter` + `onScroll`): opacity-ONLY entrances, sine, 1.25s (1.5s stats), chars stagger .015 / words .03 / side-descriptions words .025 +0.25s, items 0/.1/.2 delays, trigger at "top 95%" equivalent. NO slide-ups, NO scrub, NO pin, NO parallax. **NO Lenis** (user veto) — native scroll; nav links smooth-scroll via native `scrollIntoView({behavior:'smooth'})`.
- **Carousels**: you MAY `npm i embla-carousel` (vanilla). Config `{dragFree:true}`, prev/next circle buttons with disabled opacity .5 (§3). One shared factory, used for case studies + integrations (+ services mobile).
- Reduced-motion: every entrance collapses to visible; carousels remain draggable; add the guards berco skipped (§10.13).

## 2. Section briefs (all copy azen's; quote spec § for layout)
1. **Nav + loader (§0.7)**: glass pill nav — "azen." wordmark (blue period) + Work / Services / About / Contact + white pill **"Book a call"** → https://calendly.com/tayyib-azen/30min. Drop-in at t=0.5s. Loader: "azen." gradient-shimmer (§0.7 loader), exit 0.5s→0.75s; keep our sessionStorage once-per-session skip. On exit, dispatch `window.dispatchEvent(new CustomEvent('azen:reveal'))`.
2. **Hero (§1 layout, LIVE 3D)**: 100vh; the EXISTING `#hero-3d` canvas becomes the centered **60%×60% wrapper** with berco's 4-way gradient vignette divs fading it into #020202 (mobile: full-width 45% height, cover). Copy layout per §1: H1 left 57.5% — "Autonomous **AI systems** / built for / operations." (chars .015 from t≈0.6s); CTA row "Explore the systems" + 2.5vw circle-arrow → smooth-scroll #services; bottom-left label "Products & Services" + azen tagline; right column pushed 40vh: outlined pill "AI Infrastructure" + "Custom automation, / deployed in weeks." Mobile hides right column.
   **hero3d.js — MINIMAL touches only (2 changes, nothing else)**: (a) retune scene background gradient + fog terminus to the #020202 family so the vignette blend is seamless; (b) start the camera intro on the `azen:reveal` event (fallback: auto-start after 2s if no event). Do NOT alter chip geometry/materials/choreography/debug handle.
3. **About + metrics (§2)**: azen statement h1 (words .03) with bold spans on "autonomous", "infrastructure", "outcomes"; 4-stat band 1:1 real numbers: 85% Fewer order errors / 78% Calls resolved by AI / 90% Faster sourcing / 24/7 Autonomous operations. Icons lucide-style inline SVGs (draw them, don't install a package). Static numbers, no count-up (§2).
4. **Case-study carousel (§3 geometry + §4 flip)**: eyebrow "Our Work", h1 "Real clients. / Real **numbers**.", right desc fresh line. 6 cards: Prep Point / Superior Accounting / TTT Departmentals / Little Oaks / Zia Ul Ummah / Azen EYOS — name grey, outcome white ("85% fewer order errors." etc). Card face: the big metric numeral as the visual (3.5vw+, vignetted) — do NOT hotlink client logos we don't have. Plus-circle = REAL LINK to its case-study page (keep all case-study-*.html working). Card flips (§4 mechanics, rotateX 1s) to quote placeholder + "See full build →".
5. **Founder (§4, single person)**: two squares — card A: `founder.webp` **IN COLOUR** (user preference overrides berco B&W; adaptive text = white family on the dark photo), name "Tayyib Arbab", role "Founder, Systems Engineer", flips to short bio. Card B: the founder-note typographic card on masked-grid ("Most agencies sell you overpriced solutions you don't need…" quote we already use), no flip. Eyebrow "Founder".
6. **Services (§5)**: 4 cards at **2×2, 39.5vw × 30vh** (spec-recommended): Autonomous Agents / System Orchestration / Enterprise Retrieval / Optimization & Integration, each icon + grey 2-liner (reuse existing azen copy, tightened). Keep hover swell scale(1.05)+glow VERBATIM (§5 signature). One data source; mobile = Embla carousel of the same data.
7. **Integrations (§6)**: same carousel component, id `#stack`. Eyebrow "Integrations", h1 "The stack / we deploy **every day**.", fresh right description. 7 cards as TEXT WORDMARKS (grey Hanken 600, vignetted — no third-party logo files): Shopify · Meta · Slack · Monday.com · Figma · Fireflies.ai · GitHub, each with a fresh azen 2-liner.
8. **Mindset sequence (§7, LIVE 3D not video)**: the framed split panel (border+inner-glow frame, text 45% / visual 55%). Right side: NEW `src/chip-panel.js` — a second, SIMPLIFIED Three.js scene (square canvas in the frame, 4-way vignette): same chip material recipe as hero3d, running an AUTOMATIC slow ping-pong of the lid-lift/reassemble cycle (~8s pendulum, sine), no scroll/pointer deps, IntersectionObserver-parked, DPR≤2, its own tiny renderer (~150 lines; import the shared look, keep draw calls <20 — you may extract shared material/geometry builders from hero3d.js into src/chip/common.js and import from both, WITHOUT changing hero behavior). Copy: eyebrow "Our Mindset", h1 "Systems that **run themselves** / while you build." + azen paragraph.
9. **FAQ (§9 new-section table)**: 80vw rows, question `big-description` white, white/10 dividers, plus-circle rotates 45° on open, answer fades 1.25s. Reuse our existing 8 Q/A copy verbatim. Eyebrow "FAQ".
10. **Contact/footer (§8)**: left 30%: Chat (hello@azen.io) / Based in London / Book (Calendly). Right 70% card: h1 "Got bottlenecks? / We've got **systems**.", underlined inputs (Name / Company / Project), white pill submit **"Book a call"** → opens Calendly link (primary) and mailto:hello@azen.io fallback built from fields (include an email input — fix berco's bug §8). "© 2026 Azen Digital. All Rights Reserved" bottom-left. Mobile column-reverse.

## 3. Files & hygiene
- Rewrite: `index.html` (keep <head> meta/OG/schema; keep case-study-*.html untouched), `styles.input.css` (fresh berco-system layer; you may prune dead sections but keep tokens/utilities case-study pages depend on — grep them first), `src/enhance.js` (new entrance engine), `src/hero3d.js` (the 2 permitted touches), new `src/chip-panel.js` (+ optional `src/chip/common.js`).
- Old sections/effects to DELETE from index.html: marquee, spotlight, grain, scroll-progress bar, ghost numerals, stat corner-tick cards, work-card grid, cmp-table, price-cards, tmn-cards, cta-glow + horizon + rings, footer ghost wordmark. (Their CSS may remain if case pages use shared classes — verify with grep before pruning.)
- Build: `./node_modules/.bin/tailwindcss -i styles.input.css -o styles.css --minify` AFTER edits; bump `styles.css?v=` to a fresh value in index.html (current: 20260717a).
- Verify per the QA techniques that work here (pane may be hidden): server on :5173 (a stale node may hold the port — it's Vite watching this repo, so HMR serves your edits; check `curl` + look for your new markup in the response), zero console errors, `window.__azenChip` intact, entrance classes present, all 6 case links 200. Screenshot only if pane renders.
- NO commits. Final report ≤45 lines: per-section status, deviations flagged, verify evidence.
